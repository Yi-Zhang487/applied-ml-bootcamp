# applied-ml-bootcamp

Goal: build a practical applied-ML project from scratch (data -> training -> evaluation -> delivery).
This repo starts with a minimal engineering skeleton.

## How to run (placeholder)
1) pip install -r requirements.txt
2) Put raw data into data/
3) Add code into src/
